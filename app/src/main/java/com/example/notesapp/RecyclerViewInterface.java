package com.example.notesapp;

/***
 * a new interface to add a method that responsible for user click
 * on each images on recycler view
 * ***/
public interface RecyclerViewInterface {
    void onItemClick(int position);
}
